# AI 助手指引

## 提交规范

在运行 `git commit` 之前，**必须**阅读并遵守 `.ai/commit.md` 中的规则：

1. 提交格式：`<type>: <subject>` + Description（中文）
2. **必须**添加 `Co-authored-by`，身份从 `.ai/commit.md` 的已注册表格中选择
3. 首次见到的 AI 必须先注册自己到 `.ai/commit.md` 的已注册表格中

## 项目知识

- `.ai/memory/` 目录包含开发备忘，提交或修改前建议查阅
- 如需添加新知识，请更新对应的 memory 文件
